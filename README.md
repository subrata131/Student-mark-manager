# Student-mark-manager
Student Mark Manager is a Python program that lets you store, view, update, search, and delete student records directly from your terminal. All data is automatically saved to a text file so nothing is lost when you close the program.
